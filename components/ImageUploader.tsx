import { useRef, useState } from "react";
interface Props { fileName?: string; onFile: (file: File) => void }
export function ImageUploader({ fileName, onFile }: Props) { const input = useRef<HTMLInputElement>(null); const [drag, setDrag] = useState(false); const choose = (files: FileList | null) => files?.[0] && onFile(files[0]); return <div className={`drop ${drag ? "drag" : ""}`} onDragOver={e => {e.preventDefault();setDrag(true)}} onDragLeave={() => setDrag(false)} onDrop={e => {e.preventDefault();setDrag(false);choose(e.dataTransfer.files)}}><input ref={input} hidden type="file" accept="image/jpeg,image/png,image/webp,image/heic,image/heif,.jpg,.jpeg,.png,.webp,.heic,.heif" onChange={e => choose(e.target.files)} /><div className="uploadIcon">↥</div><strong>{fileName || "Drop your portrait here"}</strong><span>{fileName ? "Click to replace this image" : "or choose from your device"}</span><button type="button" className="secondary" onClick={() => input.current?.click()}>Choose photo</button><small>JPG, PNG, WEBP, HEIC · Max 75 MB</small></div> }
